// empty() method checks whether the string is empty or not
#include <iostream>
#include <string>
using namespace std;

int main()
{
    string str = "Hello";
    cout << "checks whether the string is empty or not: " << str.empty() << endl;

    return 0;
}
