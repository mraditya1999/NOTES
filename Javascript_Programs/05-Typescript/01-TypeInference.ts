let greet:string = "Hello World";
let userId:number = 123;
let isLoogedIn:boolean = false;

console.log(greet);
console.log(userId);
console.log(isLoogedIn);

// We can't assign other data type values to its defined data type.
// Type Inference is not compulsary in variable but it is compulsary in case of functions