// IN ENUMS CASES WHERE WE want to only give some RESTRICTIVE OPTIONS only ,ENUMS ARE USED TO SELECT B/W ONE OF THEM
const enum SeatChoice {
  AISLE,
  MIDDLE,
  WINDOW,
}

const mySeat = SeatChoice.AISLE; // it will give only above three options
